// Iterate with while loops. A while loop runs as long as a specified condition is true, and stops when it is no longer true.

var myArray = [];

let i = 0;
while (i < 5) {
  myArray.push(i);
  i++;
}

console.log(myArray);
